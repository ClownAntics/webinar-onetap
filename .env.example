# ---- Zoom Server-to-Server OAuth (Marketplace app) ----
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
# Shared host account that owns the webinars
ZOOM_HOST_USER_ID=service@facepaint.com

# ---- App Supabase ("OneStack" instance) — config + logging ----
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# ---- Sales Supabase (read-only af-sales-research / TeamDesk mirror) — revenue (§4a) ----
# project ref: rilhgeshkypbcckedaoh
SALES_SUPABASE_URL=
SALES_SUPABASE_KEY=

# ---- Omnisend ----
OMNISEND_API_KEY=

# ---- Admin auth: Supabase Auth + Google (employee login) ----
# Public Supabase creds (browser-safe). Reuse the project that already has the
# Google provider configured (af-tag-review uses rilhgeshkypbcckedaoh).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Comma-separated employee email domains allowed into /admin.
ADMIN_ALLOWED_DOMAINS=clownantics.com,facepaint.com

# ---- Misc ----
# Public base URL used in Omnisend links / calendar events
NEXT_PUBLIC_SITE_URL=https://webinars.facepaint.com
